---
title: 'Get All Suppression Entries'
sidebarTitle: 'Get All Suppression Entries'
openapi: 'POST /suppression-groups/entries/all'
---

<RequestExample>

```bash cURL
curl --request POST \
  --url 'https://api.autosend.com/v1/suppression-groups/entries/all' \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "groupId": "sg_abc123def456",
    "reason": "unsubscribed",
    "isGlobal": false
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.autosend.com/v1/suppression-groups/entries/all",
    headers={
        "Authorization": "Bearer <token>",
        "Content-Type": "application/json",
    },
    json={
        "groupId": "sg_abc123def456",
        "reason": "unsubscribed",
        "isGlobal": False,
    },
)
print(response.json())
```

```javascript JavaScript
const response = await fetch(
  "https://api.autosend.com/v1/suppression-groups/entries/all",
  {
    method: "POST",
    headers: {
      Authorization: "Bearer <token>",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      groupId: "sg_abc123def456",
      reason: "unsubscribed",
      isGlobal: false,
    }),
  }
);
const data = await response.json();
console.log(data);
```

```php PHP
<?php
$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, "https://api.autosend.com/v1/suppression-groups/entries/all");
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode([
    "groupId"  => "sg_abc123def456",
    "reason"   => "unsubscribed",
    "isGlobal" => false,
]));
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    "Authorization: Bearer <token>",
    "Content-Type: application/json",
]);
$response = curl_exec($ch);
curl_close($ch);
echo $response;
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
)

func main() {
    payload, _ := json.Marshal(map[string]interface{}{
        "groupId":  "sg_abc123def456",
        "reason":   "unsubscribed",
        "isGlobal": false,
    })

    req, _ := http.NewRequest("POST", "https://api.autosend.com/v1/suppression-groups/entries/all", bytes.NewBuffer(payload))
    req.Header.Set("Authorization", "Bearer <token>")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    body, _ := io.ReadAll(resp.Body)
    fmt.Println(string(body))
}
```

```java Java
import java.net.URI;
import java.net.http.*;

public class Main {
    public static void main(String[] args) throws Exception {
        String body = """
            {
              "groupId": "sg_abc123def456",
              "reason": "unsubscribed",
              "isGlobal": false
            }
            """;

        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create("https://api.autosend.com/v1/suppression-groups/entries/all"))
            .header("Authorization", "Bearer <token>")
            .header("Content-Type", "application/json")
            .POST(HttpRequest.BodyPublishers.ofString(body))
            .build();

        HttpResponse<String> response = HttpClient.newHttpClient()
            .send(request, HttpResponse.BodyHandlers.ofString());
        System.out.println(response.body());
    }
}
```

```ruby Ruby
require "net/http"
require "uri"
require "json"

uri = URI("https://api.autosend.com/v1/suppression-groups/entries/all")
req = Net::HTTP::Post.new(uri)
req["Authorization"] = "Bearer <token>"
req["Content-Type"] = "application/json"
req.body = {
  groupId:  "sg_abc123def456",
  reason:   "unsubscribed",
  isGlobal: false,
}.to_json

response = Net::HTTP.start(uri.host, uri.port, use_ssl: true) { |http| http.request(req) }
puts response.body
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true,
  "data": [
    {
      "id": "entry_xyz789",
      "email": "user@example.com",
      "groupId": "sg_abc123def456",
      "reason": "unsubscribed",
      "createdAt": "2026-02-01T09:00:00.000Z"
    },
    {
      "id": "entry_abc001",
      "email": "another@example.com",
      "groupId": "sg_abc123def456",
      "reason": "unsubscribed",
      "createdAt": "2026-02-15T14:30:00.000Z"
    }
  ]
}
```

</ResponseExample>

---
#### Authorizations
<ParamField path="Authorizations" type="string | header" required>
  Bearer authentication header of the form Bearer `<token>`, where `<token>` is your auth token.
</ParamField>

#### Body
<ParamField body="groupId" type="string">
  Filter entries to a specific suppression group ID. Omit to return entries across all groups.
</ParamField>

<ParamField body="reason" type="string">
  Filter entries by suppression reason (e.g. `unsubscribed`, `bounced`, `spam_complaint`).
</ParamField>

<ParamField body="isGlobal" type="boolean">
  When `true`, returns only entries belonging to global suppression groups.
</ParamField>
