---
title: 'Bulk Unsuppress Emails'
sidebarTitle: 'Bulk Unsuppress Emails'
openapi: 'PUT /suppression-groups/emails/unsuppress'
---

<RequestExample>

```bash cURL
curl --request PUT \
  --url 'https://api.autosend.com/v1/suppression-groups/emails/unsuppress' \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "emails": ["user@example.com"],
    "groupId": "sg_abc123def456"
  }'
```

```python Python
import requests

response = requests.put(
    "https://api.autosend.com/v1/suppression-groups/emails/unsuppress",
    headers={
        "Authorization": "Bearer <token>",
        "Content-Type": "application/json",
    },
    json={
        "emails": ["user@example.com"],
        "groupId": "sg_abc123def456",
    },
)
print(response.json())
```

```javascript JavaScript
const response = await fetch(
  "https://api.autosend.com/v1/suppression-groups/emails/unsuppress",
  {
    method: "PUT",
    headers: {
      Authorization: "Bearer <token>",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      emails: ["user@example.com"],
      groupId: "sg_abc123def456",
    }),
  }
);
const data = await response.json();
console.log(data);
```

```php PHP
<?php
$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, "https://api.autosend.com/v1/suppression-groups/emails/unsuppress");
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_CUSTOMREQUEST, "PUT");
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode([
    "emails"  => ["user@example.com"],
    "groupId" => "sg_abc123def456",
]));
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    "Authorization: Bearer <token>",
    "Content-Type: application/json",
]);
$response = curl_exec($ch);
curl_close($ch);
echo $response;
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
)

func main() {
    payload, _ := json.Marshal(map[string]interface{}{
        "emails":  []string{"user@example.com"},
        "groupId": "sg_abc123def456",
    })

    req, _ := http.NewRequest("PUT", "https://api.autosend.com/v1/suppression-groups/emails/unsuppress", bytes.NewBuffer(payload))
    req.Header.Set("Authorization", "Bearer <token>")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    body, _ := io.ReadAll(resp.Body)
    fmt.Println(string(body))
}
```

```java Java
import java.net.URI;
import java.net.http.*;

public class Main {
    public static void main(String[] args) throws Exception {
        String body = """
            {
              "emails": ["user@example.com"],
              "groupId": "sg_abc123def456"
            }
            """;

        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create("https://api.autosend.com/v1/suppression-groups/emails/unsuppress"))
            .header("Authorization", "Bearer <token>")
            .header("Content-Type", "application/json")
            .PUT(HttpRequest.BodyPublishers.ofString(body))
            .build();

        HttpResponse<String> response = HttpClient.newHttpClient()
            .send(request, HttpResponse.BodyHandlers.ofString());
        System.out.println(response.body());
    }
}
```

```ruby Ruby
require "net/http"
require "uri"
require "json"

uri = URI("https://api.autosend.com/v1/suppression-groups/emails/unsuppress")
req = Net::HTTP::Put.new(uri)
req["Authorization"] = "Bearer <token>"
req["Content-Type"] = "application/json"
req.body = {
  emails:  ["user@example.com"],
  groupId: "sg_abc123def456",
}.to_json

response = Net::HTTP.start(uri.host, uri.port, use_ssl: true) { |http| http.request(req) }
puts response.body
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true,
  "data": {
    "unsuppressed": 1
  }
}
```

</ResponseExample>

---
#### Authorizations
<ParamField path="Authorizations" type="string | header" required>
  Bearer authentication header of the form Bearer `<token>`, where `<token>` is your auth token.
</ParamField>

#### Body
<ParamField body="emails" type="string[]">
  An array of email addresses to remove from suppression. Must contain at least one address if provided. Either `emails` or `ids` (or both) must be supplied.
</ParamField>

<ParamField body="ids" type="string[]">
  An array of suppression entry IDs to remove. Must contain at least one ID if provided. Either `emails` or `ids` (or both) must be supplied.
</ParamField>

<ParamField body="groupId" type="string">
  Scopes the removal to a specific suppression group. When omitted, matching entries are removed from all groups.
</ParamField>

<ParamField body="isGlobal" type="boolean">
  When `true`, removes entries from the global suppression list rather than a project-specific group.
</ParamField>
